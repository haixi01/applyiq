# ── applyiq-secret.yaml ────────────────────────────────────────────────────────
# BEFORE applying, base64-encode your real key:
#   echo -n "YOUR_GOOGLE_API_KEY_HERE" | base64
# Then replace the placeholder below.
#
# Apply with:  kubectl apply -f manifests/applyiq-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: applyiq-secret
  namespace: default
type: Opaque
data:
  # Replace with: echo -n "YOUR_ACTUAL_KEY" | base64
  GOOGLE_API_KEY: WU9VUl9BQ1RVQUxfR09PR0xFX0FQSV9LRVk=
